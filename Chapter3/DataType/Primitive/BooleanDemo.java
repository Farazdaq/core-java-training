package Chapter3.DataType.Primitive;

public class BooleanDemo {
    boolean areTheyGroup = true;

    byte a = 5;
    byte b = 2;
   boolean result = (a < b);
    public void display(){
        System.out.println("Are they Group (A) ? :" + areTheyGroup);
        System.out.println("Is (A) less Than (B) ? :" + result);
    }
}
